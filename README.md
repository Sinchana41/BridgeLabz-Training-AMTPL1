# FileInOuPrograms

* Demonstrates basic file input and output operations
* Uses:

File.WriteAllText

File.ReadAllText

StreamWriter

StreamReader

* Use Case:
Writing and reading simple text data from a file.

# FileIOUsingCSV

* Demonstrates CSV file handling
* Reads and writes Person data to a .csv file
* Converts objects ↔ CSV format manually

* Concepts Covered:

File handling

String manipulation

Object serialization (CSV style)

# FileIOUsingJson

* Demonstrates JSON file handling
* Uses System.Text.Json for serialization and deserialization
* Stores and retrieves Person objects in JSON format

* Concepts Covered:

JSON Serialization

JSON Deserialization

File persistence

